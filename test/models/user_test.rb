require 'test_helper'

class UserTest < ActiveSupport::TestCase
	
	test "should not save post without title" do 
		#user = User.new(:email => "ralph@gmail.com")
		#assert_not user.save, "Saved the user without a title"
	end
end
